"""Integration tests for the audiobook package.

Unit tests live next to the source files they test (Go-style):
    src/audiobook/domain/parser_test.py  ← unit tests for parser.py

This directory is for integration tests that span multiple modules
and cannot be attributed to a single source file — for example,
end-to-end generation workflows or CLI invocation tests.
"""
